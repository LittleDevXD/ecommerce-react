import DirectoryContainer from "../../components/directory-container/directory-container.component";

const Home = () => {
    
  
    return (
        <DirectoryContainer />
    );
};

export default Home;